# coturn — STUN + TURN for Hulls & Hellfire off-LAN multiplayer.
# Serves STUN (candidate discovery) and TURN (relay when peer-to-peer fails,
# which on carrier-grade NAT is often).

listening-port=3478
tls-listening-port=5349

# The address clients reach. Behind NAT (most VPS are not, but check), set
# external-ip=PUBLIC_IP/PRIVATE_IP so coturn advertises the right candidate.
realm=turn.v-collaborate.com
server-name=turn.v-collaborate.com

# Long-term credentials. Replace the password before deploying, and see the
# README on moving to time-limited HMAC credentials once the rendezvous exists.
lt-cred-mech
user=hhfh:CHANGE_ME_LONG_RANDOM_SECRET

# Relay port range — must match the published range in docker-compose.yml.
# 48 ports is ample for a turn-based game; each session takes one.
min-port=49152
max-port=49200

fingerprint
no-multicast-peers
no-cli
no-tlsv1
no-tlsv1_1

# Refuse to relay into private space. Without this the server is an open proxy
# into whatever network it sits on — the main way a public TURN box gets abused.
denied-peer-ip=0.0.0.0-0.255.255.255
denied-peer-ip=10.0.0.0-10.255.255.255
denied-peer-ip=127.0.0.0-127.255.255.255
denied-peer-ip=169.254.0.0-169.254.255.255
denied-peer-ip=172.16.0.0-172.31.255.255
denied-peer-ip=192.168.0.0-192.168.255.255
denied-peer-ip=::1
denied-peer-ip=fc00::-fdff:ffff:ffff:ffff:ffff:ffff:ffff:ffff

# TLS on 5349. Point these at your Let's Encrypt files; drop these two lines
# and the tls-listening-port above to run plaintext only.
cert=/etc/coturn/certs/fullchain.pem
pkey=/etc/coturn/certs/privkey.pem

# Quotas — a single abusive client should not be able to saturate the box.
user-quota=12
total-quota=1200
